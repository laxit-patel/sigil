# Editors / OS
.DS_Store
*.swp
.idea/
.vscode/

# Personal Claude Code overrides
.claude/settings.local.json

# Language directories carry their own .gitignore for build output
# (sigil-php/vendor, etc.) so each stays self-contained.

# Scratch inbox for documents handed to the project. Content gets absorbed
# into SPEC.md / model.json / fixtures, so the drop-box itself isn't tracked.
docs/
