# Editors / OS
.DS_Store
*.swp
.idea/
.vscode/

# Personal Claude Code overrides
.claude/settings.local.json

# Language directories carry their own .gitignore for build output
# (sigil-php/vendor, etc.) so each stays self-contained.
